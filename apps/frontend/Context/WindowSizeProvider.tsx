'use client'
// import useWindowSize from '@/lib/hooks/common/useWindowSize'

// import React  from 'react'

// // eslint-disable-next-line @typescript-eslint/ban-types
// type Props = {
//   children: React.ReactNode
// }

// export const WindowSizeContext = React.createContext(null)

// const WindowSizeProvider = (props: Props) => {
//   const windowSize = useWindowSize()

//   return (
//     <WindowSizeContext.Provider value={windowSize}>{props.children}</WindowSizeContext.Provider>
//   )
// }

// export default WindowSizeProvider